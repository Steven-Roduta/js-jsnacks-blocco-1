// Nomi Invitati di Gatsby
const names = [
    'Goku',
    'Vegeta',
    'Chichi',
    'Gohan',
    'Gotens',
    'Trunks',
    'Piccolo',
]
// chiedi all' utente il suo nome

const utenteName = prompt ('Put your Name');

// se l' utente puo' partecipare oppure

for ( let i = 0; i = utenteName.length ; i++){

    const nameInvited = names [i];

    if (nameInvited.length = names){
        utenteName.push (names)
        console.log(utenteName,names);
    }

}