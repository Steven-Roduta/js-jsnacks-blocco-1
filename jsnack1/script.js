// chiedo l' utente di inserire due numeri in successione
const utente = Number (prompt ('Inserisci due numeri in successione'));

// il software deve stampare il numero maggiore

const utenteSoft = Number ( prompt ('Stampa il numero maggiore'));
console.log (utente , utenteSoft);

if (utente >= utenteSoft ) {
   
    result = 'Stampa'
} else {

    result = 'non Stampare'
}